# Secrets never belong in the repo (scripts create these locally on the Pi)
.env
.env.local
*.key
*.pem
# OS / editor cruft
.DS_Store
*.swp
*~

# Quarto render output for the book source (the final PDF is committed at repo root)
book/_book/
book/.quarto/
